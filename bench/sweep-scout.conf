# Llama-4-Scout add-on (Meta access approved). Natively multimodal -> runs the
# FULL tiered eval like the VL models (TIERS empty = all; commercial enabled
# by the launcher env). 109B MoE, BF16 ~218GB, TP=8.
meta-llama/Llama-4-Scout-17B-16E-Instruct|8|8|16384|230Gi||
