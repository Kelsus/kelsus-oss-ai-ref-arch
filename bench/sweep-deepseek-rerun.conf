# DeepSeek-V3.1 quality — stability-first config (attempt after SE-17 probe fix).
# Prior failures were vLLM going connection-refused under load (readiness probe
# flapping it out of Service endpoints, SE-17), not the gateway. Now: tolerant
# vLLM readiness probe (in the template) + shorter context (8192) for KV
# headroom + low scorer concurrency (launcher sets WORKERS=2) + a 10-item smoke
# gate before the full run. p5-pinned -> 8x H100.
# MODEL|TP|GPUS|MAXLEN|CACHE|TIERS|EXTRA
QuantTrio/DeepSeek-V3.1-AWQ|8|8|8192|380Gi|clean-digital|--enable-expert-parallel
